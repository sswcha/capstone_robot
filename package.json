{
  "name": "webserver",
  "version": "1.0.0",
  "description": "webserver for pi",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sean",
  "license": "ISC",
  "dependencies": {
    "onoff": "^6.0.3",
    "socket.io": "^4.7.5"
  }
}
